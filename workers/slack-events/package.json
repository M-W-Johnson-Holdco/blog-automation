{
  "name": "blog-automation-slack-events",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:github-token": "wrangler secret put GITHUB_TOKEN",
    "secret:slack-signing:peachtree": "wrangler secret put SLACK_SIGNING_SECRET_PEACHTREE",
    "secret:slack-signing:tc": "wrangler secret put SLACK_SIGNING_SECRET_TC"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
